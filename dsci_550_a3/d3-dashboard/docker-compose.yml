version: "3.9"  # Use latest Compose syntax

services:
  dashboard:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: d3-dashboard
    ports:
      - "5173:5173"
    volumes:
      - .:/app
      - /app/node_modules  # Prevent node_modules sync issues
    command: npm run dev -- --host